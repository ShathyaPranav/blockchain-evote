{
  "name": "blockchain-evote",
  "version": "1.0.0",
  "description": "Minimal blockchain voting demo using Hardhat + Solidity + MetaMask + Flask",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "node": "npx hardhat node"
  },
  "keywords": ["blockchain", "voting", "solidity", "hardhat", "ethereum"],
  "author": "Ishanvi Kaushik",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
